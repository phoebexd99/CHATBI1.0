# Demo Capture Checklist

Record both live and replay modes. Show the default GMV question, retrieved metric/schema evidence, generated SQL, workflow trace, scalar result and chart. Then show the knowledge center and evaluation center. Blur machine-specific paths and verify the browser network panel contains no secrets. Store future screenshots under `docs/assets/` and a short narration script beside them.

